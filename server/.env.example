# Origines autorisees en CORS (le frontend Next.js deploye sur Vercel + previews + dev local).
# Plusieurs valeurs separees par des virgules. Laisser vide pour tout autoriser (dev uniquement).
CLIENT_ORIGIN=https://ton-projet.vercel.app,http://localhost:3000

# Railway/Render/Fly.io fournissent PORT automatiquement ; a definir seulement en local si besoin.
# PORT=3001
